import 'package:flutter/material.dart';

import '../_placeholder_screen.dart';

/// Sales Return screen (opened from the "More" sheet) — built in
/// Step 13.
class SalesReturnScreen extends StatelessWidget {
  const SalesReturnScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(title: 'Sales Return', stepLabel: 'Step 13');
  }
}
