import 'package:flutter/material.dart';

import '../_placeholder_screen.dart';

/// Purchase Return screen (opened from the "More" sheet) — built in
/// Step 14.
class PurchaseReturnScreen extends StatelessWidget {
  const PurchaseReturnScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PlaceholderScreen(title: 'Purchase Return', stepLabel: 'Step 14');
  }
}
